import './style.css';

const {
  default: clickAcessories, Arrows, moveImages, setImageCarousel, fiveSeconds,
} = require('./DOM');

clickAcessories();
setImageCarousel();
moveImages();
Arrows();
fiveSeconds();
